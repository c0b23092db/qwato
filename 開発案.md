# 追加したい機能

## タグをつける設定
```toml
[command.default]
tags = ["daily","memo"]
```
```md
- #daily #memo タグがつく
```

## コマンド一覧を表示
```bash
qwa --command
```
使えるコマンドを表示する。

## サマリーコマンド
```bash
qwa --summary
```
今日更新されたファイルのリストを出力する。

## タグの検索コマンド
```bash
qwa --tag daily
```
```bash
qwa --tag daily memo --list 100
```

## チェックボックスをつけるコマンド
```bash
qwa --done 3
```
新しい淳から見て3つ目のチェックボックスをつける。

## 「最後のメモ」を再編集するコマンド
```bash
qwa --last-edit aaa
qwa -le aaa
```

## 日付範囲フィルタ付き一覧
```bash
qwa --note --from 2026-08-01 --to 2026-08-07
```

## エクスポート／インポート
```bash
qwa --note --export json
```

## インタラクティブ
```bash
qwa --interactive
```
インタラクティブに入力できるようにする。

## URLからタイトルを取得する設定
前提: ripgrep,sd
```toml
auto_title = true
```
```bash
curl -sL "https://zenn.dev/dev_jp/articles/da537a095f13da" | rg -oi "<title.*>([^<]*)</title>" | sd "<[^>]*>" ""
```
```md
- [Title](https://example.com)
- https://example.com 
```

# バグの修正

## list.rs
- limit達成時に早期終了させる

